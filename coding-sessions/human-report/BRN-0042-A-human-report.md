# BRN-0042-A Human Report

## Why This Mattered

One adjacent provider-free contract still generated the quote-copying answer
shape removed by the parent ticket, causing the core suite to enter a repair.

## What Changed

The fixture now selects its returned memory by short number and declares its
semantic use with a disposition and rationale. The parent adapter, not the
fixture model response, attaches the canonical ID and exact returned excerpt.

## What I Should Know

The focused file passes 8/8, the core suite passes 89/89, ticket checks pass,
and the diff contains no product code. No paid provider or private artifact was
used.

## What To Check

The fixture must still commit on its first attempt, expose the host-bound exact
excerpt, and leave the newer row as unresolved telemetry.

## Recommended Next Move

The founder authorized acceptance and merge after the requested change was
ready. This narrow child requires no independent review and is accepted for
integration into BRN-0042; the R2 parent still requires fresh independent
review before its own acceptance.
