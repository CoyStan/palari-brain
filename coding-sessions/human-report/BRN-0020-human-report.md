# BRN-0020 Human Report

## Why This Mattered

The pre-change Sol test began after Luna had already searched memory. It showed
whether a stronger final turn could use delivered evidence, but could not test
the new retrieval planner. This comparison begins from the same sealed memory
state before search.

## What Changed

No product or historical result changed. A private one-shot launcher now copies
the exact four old memory databases, asks the same four questions with Sol/low,
and uses the accepted planning, timeline/read, Gemini semantic, Ettin, and
evidence-commitment path. It runs no writer and no official benchmark judge.

## What I Should Know

The run is not authorized yet. The `$0.50` / `$8.17192994` limits are proposed
caps only. Offline verification proves the required user statements are really
inside the sealed memories, so a miss will now be attributable to current
retrieval/model behavior rather than absent source data.

The five recall/use metrics remain separate. A model's consequence field is a
declaration; independent judgment is still required to call evidence
materially used.

## What To Check

- Source databases are copied exactly and never rebuilt or repaired.
- No answer text or benchmark route appears in product code or the model input.
- Every request reserves under the hard cap before transport.
- Miami's cross-city preference transfer stays temporary and canonical rows
  hash identically before and after.
- The old 6/10 and pre-change Sol result remain unchanged.

## Recommended Next Move

Obtain fresh independent pre-dispatch review. If clean, request exact founder
authority for identity `j4-sol-frozen-failures-post-architecture-v1` under the
proposed `$0.50` fresh / `$8.17192994` cumulative accounted caps.
